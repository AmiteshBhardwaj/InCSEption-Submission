{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -d zebra-synapse ]; then cd zebra-synapse; fi; npm ci",
  "buildCommand": "if [ -d zebra-synapse ]; then cd zebra-synapse && npm run build && rm -rf ../dist && mkdir -p ../dist && cp -R dist/. ../dist/; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
